{
  "name": "demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc --outDir dist/ --module commonjs --moduleResolution node --target es5 --strict true index.ts",
    "pretest": "npm run build",
    "test": "node dist/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "circleci-api": "../"
  },
  "devDependencies": {
    "ts-node": "10.3.0",
    "typescript": "4.4.4"
  }
}
